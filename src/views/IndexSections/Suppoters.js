import React, { Component } from 'react';

import { Card, Button, Container, Row, Col } from 'reactstrap';

export default class Suppoters extends Component {
  render() {
    return (
      <Container className='shape-container d-flex align-items-center py-lg'></Container>
    );
  }
}
